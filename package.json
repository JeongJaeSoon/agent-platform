{
  "name": "agent-platform",
  "private": true,
  "packageManager": "bun@1.3.10",
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/adapters/execution/*",
    "packages/adapters/runtimes/*"
  ],
  "scripts": {
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "biome check .",
    "test": "bun test tests packages apps",
    "test:spike:94s-91": "bun run --cwd spikes/94s-91 test:contract",
    "check": "bun run typecheck && bun run lint && bun run test",
    "keys": "docker compose --profile apps exec -T api bun run apps/control-host/src/api/keys.ts",
    "grants": "docker compose --profile apps exec -T api bun run apps/control-host/src/api/grants.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.4",
    "@types/bun": "1.3.10",
    "typescript": "5.9.2"
  }
}
