# The example repository (config/repositories.yaml), trusted for the real
# profile alone (94S-373).
repositories:
  sample-app:
    url: http://gitea:3000/agent/sample-app.git
    branch: main
    profiles: [claude-coding-real]
